
package model.gestionProcesos;

import java.util.ArrayList;
import java.util.List;
import java.util.Random;
import test.Generator;

public class ListaErrores {
    /*private static List<Error> lista;
    
    private static void construir() {
        lista = new ArrayList<Error>();
        
        lista.add(new Error(8246 , "Bucle infinito"));
        lista.add(new Error(1767 , "Division entre 0"));
        lista.add(new Error(998 , "Acceso invalido de memoria"));
    }
    
    public static Error getError() {
        if(lista == null) {
            construir();
        }
        int seed = (int) System.currentTimeMillis();
        Random random = new Random(seed);
        return lista.get(random.nextInt(3));
    }*/
    
}
